No program memory space for this processor.
--- grbl-master/print.c	2014-06-07 14:18:58.000000000 -0700
+++ grbl-master/print.c	2014-06-07 14:24:55.000000000 -0700
@@ -37,9 +37,8 @@
 // Print a string stored in PGM-memory
 void printPgmString(const char *s)
 {
-  char c;
-  while ((c = pgm_read_byte_near(s++)))
-    serial_write(c);
+  while (*s)
+    serial_write(*s++);
 }
 
 // void printIntegerInBase(unsigned long n, unsigned long base)
@@ -63,12 +62,12 @@
 // 			'A' + buf[i - 1] - 10);
 // }
 
-void print_uint8_base2(uint8_t n)
+static void print_base2(uint32_t n, int bits)
 { 
-	unsigned char buf[8];
+	unsigned char buf[bits];
 	uint8_t i = 0;
 
-	for (; i < 8; i++) {
+	for (; i < bits; i++) {
 		buf[i] = n & 1;
 		n >>= 1;
 	}
@@ -77,6 +76,17 @@
 		serial_write('0' + buf[i - 1]);
 }
 
+
+void print_uint8_base2(uint8_t n)
+{ 
+    print_base2(n, 8);
+}
+
+void print_uint32_base2(uint32_t n)
+{ 
+    print_base2(n, 32);
+}
+
 static void print_uint32_base10(unsigned long n)
 { 
   unsigned char buf[10]; 
--- grbl-master/print.h	2014-06-07 14:28:21.000000000 -0700
+++ grbl-master/print.h	2014-06-07 14:28:34.000000000 -0700
@@ -32,7 +32,8 @@
 void printInteger(long n);
 
 void print_uint8_base2(uint8_t n);
+void print_uint32_base2(uint8_t n);
 
 void printFloat(float n);
 
-#endif
\ No newline at end of file
+#endif
